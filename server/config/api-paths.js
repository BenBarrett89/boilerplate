const API = 'api'
const API_PATH = `/${API}`

const DOMAIN = 'domain'
const DOMAIN_PATH = `${API_PATH}/${DOMAIN}`

const PRODUCT = 'product'
const PRODUCT_PATH = `${API_PATH}/${PRODUCT}`

module.exports = {
  API,
  API_PATH,
  DOMAIN,
  DOMAIN_PATH,
  PRODUCT,
  PRODUCT_PATH
}
